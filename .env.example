# Formspree — "Send a message" contact form (browser / Vercel client bundle)
# Optional: omit to use the default form URL baked into the app.
# In Vercel: Project → Settings → Environment Variables → add for Production (and Preview if needed)
NEXT_PUBLIC_FORMSPREE_URL=https://formspree.io/f/mdawvbrw
